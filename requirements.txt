# ─── Core training ───────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0          # needed for some torch utilities

# ─── Data / evaluation ───────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0          # confusion_matrix, classification_report
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
requests>=2.31.0             # dataset download
scipy>=1.11.0                # Butterworth filter for gravity removal (preprocessing)

# ─── Data collection (PC side) ───────────────────────────────────────────────
pyserial>=3.5               # serial comms with Arduino (collect_data.py)

# ─── ONNX export & validation ────────────────────────────────────────────────
onnx>=1.15.0
onnxruntime>=1.16.0          # CPU inference for ONNX validation

# ─── ONNX → TFLite conversion ────────────────────────────────────────────────
# onnx2tf converts ONNX → TF SavedModel (handles Conv1d transposition)
onnx2tf>=1.22.0
tensorflow>=2.13.0           # TFLiteConverter for FP32 and int8 PTQ
tf-keras>=2.14.0             # Keras 3 compatibility shim (required by onnx2tf)
onnx-graphsurgeon>=0.3.27    # graph manipulation (onnx2tf dependency)
sng4onnx>=1.0.4              # shape normalisation (onnx2tf dependency)
